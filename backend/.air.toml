root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/api"
  bin = "tmp/main"
  delay = 200
  exclude_dir = ["tmp", "vendor", "node_modules"]
  include_ext = ["go", "mod", "sum", "env"]

[log]
  time = true
